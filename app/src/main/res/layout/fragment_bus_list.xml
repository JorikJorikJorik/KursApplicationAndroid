<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/tools"
        xmlns="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.example.jorik.kursapplicationandroid.Model.Enum.Rights"/>

        <import type="android.view.View"/>

        <variable
            name="busViewModel"
            type="com.example.jorik.kursapplicationandroid.ViewModel.BusViewModel"/>
    </data>


    <android.support.design.widget.CoordinatorLayout
        android:id="@+id/bus_coordination_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <android.support.v4.widget.SwipeRefreshLayout
            android:id="@+id/refresh_bus_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >

            <android.support.v7.widget.RecyclerView
                android:id="@+id/bus_recycler"
                android:layout_width="380dp"
                android:layout_height="573dp"
                android:visibility="@{busViewModel.visibleRecyclerLayout}"
                app:adapter='@{busViewModel.busAdapter}'>

            </android.support.v7.widget.RecyclerView>

        </android.support.v4.widget.SwipeRefreshLayout>

        <TextView
            android:id="@+id/error_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignStart="@+id/button"
            android:layout_centerVertical="true"
            android:layout_gravity="center"
            android:text="@{busViewModel.errorString}"
            android:visibility="@{busViewModel.visibleError}"
            android:textAppearance="?android:attr/textAppearanceLarge"/>

        <ProgressBar
            android:id="@+id/bus_progress"
            style="?android:attr/progressBarStyleLarge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:layout_gravity="center"
            android:visibility="@{busViewModel.visibleProgress}"/>


        <android.support.design.widget.FloatingActionButton
            android:id="@+id/bus_add_fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|right"
            android:layout_margin="@dimen/fab_margin"
            android:src="@android:drawable/ic_input_add"
            android:visibility="@{busViewModel.visibleFAB}"/>

    </android.support.design.widget.CoordinatorLayout>
</layout>
